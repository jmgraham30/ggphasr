% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nullclines.R
\name{gg_nullclines}
\alias{gg_nullclines}
\title{Add nullclines to a phase plane plot}
\usage{
gg_nullclines(
  deriv,
  xlim,
  ylim,
  system = c("two.dim", "one.dim"),
  parameters = NULL,
  n_points = 250L,
  x_color = "#d73027",
  y_color = "#4575b4",
  x_linetype = "solid",
  y_linetype = "dashed",
  linewidth = 0.75,
  add_legend = TRUE,
  legend_position = "right"
)
}
\arguments{
\item{deriv}{A function describing the ODE system, in Convention A or B.
See \link{ggphasr-package} for details.}

\item{xlim}{Numeric vector of length 2. x-axis range. Should match the
\code{xlim} passed to \code{\link[=gg_flow_field]{gg_flow_field()}}.}

\item{ylim}{Numeric vector of length 2. y-axis range. Should match the
\code{ylim} passed to \code{\link[=gg_flow_field]{gg_flow_field()}}.}

\item{system}{Character: \code{"two.dim"} (default) or \code{"one.dim"}.}

\item{parameters}{Parameter vector or list passed to \code{deriv}.}

\item{n_points}{Integer. Grid resolution for nullcline computation.
Default: \code{250}. Higher values give smoother curves.}

\item{x_color}{Character. Color of the x-nullcline (where \eqn{dx/dt = 0}).
Default: \code{"#d73027"} (red).}

\item{y_color}{Character. Color of the y-nullcline (where \eqn{dy/dt = 0}).
Default: \code{"#4575b4"} (blue).}

\item{x_linetype}{Character or integer. Line type for the x-nullcline.
Default: \code{"solid"}.}

\item{y_linetype}{Character or integer. Line type for the y-nullcline.
Default: \code{"dashed"}.}

\item{linewidth}{Numeric. Line width for both nullclines. Default: \code{0.75}.}

\item{add_legend}{Logical. If \code{TRUE}, adds a legend entry for each
nullcline. Default: \code{TRUE}.}

\item{legend_position}{Character string or numeric vector of length 2.
Controls legend placement. One of \code{"right"} (default), \code{"left"},
\code{"top"}, \code{"bottom"}, \code{"none"}, \code{"inside"} (top-right corner of the
panel, compact styling), or a numeric vector \code{c(x, y)} with values
in \verb{[0, 1]} for a custom inside position.}
}
\value{
A list of \link[ggplot2:ggplot2]{ggplot2} layer objects. Add to a ggplot with \code{+}.
}
\description{
Computes and adds the nullclines of a one- or two-dimensional autonomous
ODE system to an existing phase plane plot. Returns a list of
\code{\link[ggplot2:layer]{ggplot2::layer()}} objects that can be added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}
object with \code{+}.
}
\details{
For a 2D system \eqn{dx/dt = f(x,y)}, \eqn{dy/dt = g(x,y)}:
\itemize{
\item \strong{x-nullcline}: the curve(s) where \eqn{f(x,y) = 0}
\item \strong{y-nullcline}: the curve(s) where \eqn{g(x,y) = 0}
}

For a 1D system \eqn{dy/dt = f(y)}, the nullclines are the equilibrium
points (where \eqn{f(y) = 0}), drawn as horizontal lines on the phase
portrait.
}
\examples{
# Standard workflow: flow field + nullclines
gg_flow_field(
  ode_lotka_volterra,
  xlim       = c(0, 5),
  ylim       = c(0, 5),
  parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
) +
  gg_nullclines(
    ode_lotka_volterra,
    xlim       = c(0, 5),
    ylim       = c(0, 5),
    parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
  )

# Customize nullcline appearance
gg_flow_field(
  ode_competition,
  xlim       = c(0, 15),
  ylim       = c(0, 15),
  parameters = c(r1=1, r2=1, K1=10, K2=10, a12=0.5, a21=0.5)
) +
  gg_nullclines(
    ode_competition,
    xlim       = c(0, 15),
    ylim       = c(0, 15),
    parameters = c(r1=1, r2=1, K1=10, K2=10, a12=0.5, a21=0.5),
    x_color    = "forestgreen",
    y_color    = "darkorange",
    linewidth  = 1
  )

}
\seealso{
\code{\link[=gg_flow_field]{gg_flow_field()}}, \code{\link[=gg_trajectory]{gg_trajectory()}}
}
