% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifolds.R
\name{gg_manifolds}
\alias{gg_manifolds}
\title{Draw stable and unstable manifolds of a saddle point}
\usage{
gg_manifolds(
  deriv,
  equilibrium,
  parameters = NULL,
  eq_classified = NULL,
  draw_stable = TRUE,
  draw_unstable = TRUE,
  t_manifold = 10,
  t_steps = 500L,
  epsilon = 1e-04,
  method = "lsoda",
  stable_color = "#4575b4",
  unstable_color = "#d73027",
  linewidth = 1,
  stable_linetype = "dashed",
  unstable_linetype = "solid",
  add_arrows = TRUE,
  arrow_size = 0.35,
  add_legend = TRUE,
  h = 1e-06
)
}
\arguments{
\item{deriv}{A function describing the 2D ODE system, in Convention A
or B. See \link{ggphasr} for details.}

\item{equilibrium}{Numeric vector of length 2 giving the saddle point
coordinates. Typically obtained from \code{\link[=find_equilibrium]{find_equilibrium()}}.}

\item{parameters}{Parameter vector or list passed to \code{deriv}.}

\item{eq_classified}{A data frame returned by
\code{\link[=classify_equilibrium]{classify_equilibrium()}}, or \code{NULL} (default). When supplied,
the Jacobian is read from this object rather than recomputed.}

\item{draw_stable}{Logical. Whether to draw the stable manifold
(trajectories approaching the saddle). Default: \code{TRUE}.}

\item{draw_unstable}{Logical. Whether to draw the unstable manifold
(trajectories leaving the saddle). Default: \code{TRUE}.}

\item{t_manifold}{Numeric. Integration time for each manifold branch.
Default: \code{10}. Increase for manifolds that curve far from the saddle.}

\item{t_steps}{Integer. Number of integration steps per branch.
Default: \code{500}.}

\item{epsilon}{Numeric. Distance from the saddle to the seed points
along each eigenvector. Default: \code{1e-4}. Decrease if the saddle is
near boundaries or other equilibria.}

\item{method}{Character. deSolve integration method. Default: \code{"lsoda"}.}

\item{stable_color}{Character. Color of the stable manifold.
Default: \code{"#4575b4"} (blue).}

\item{unstable_color}{Character. Color of the unstable manifold.
Default: \code{"#d73027"} (red).}

\item{linewidth}{Numeric. Line width for both manifolds. Default: \code{1}.}

\item{stable_linetype}{Character or integer. Line type for the stable
manifold. Default: \code{"dashed"}.}

\item{unstable_linetype}{Character or integer. Line type for the unstable
manifold. Default: \code{"solid"}.}

\item{add_arrows}{Logical. Whether to add direction arrows along the
manifold curves. Default: \code{TRUE}.}

\item{arrow_size}{Numeric. Arrow head size in lines. Default: \code{0.35}.}

\item{add_legend}{Logical. Whether to add a legend entry for each
manifold. Default: \code{TRUE}.}

\item{h}{Numeric. Finite-difference step size for Jacobian computation
when \code{eq_classified = NULL}. Default: \code{1e-6}.}
}
\value{
A list of \link[ggplot2:ggplot2]{ggplot2} layer objects. Add to a ggplot with \code{+}.
Returns an empty list (invisibly) with a warning if the equilibrium
is not a saddle point.
}
\description{
Computes and draws the stable and/or unstable manifolds of a saddle
point for a two-dimensional autonomous ODE system. Returns a list of
\link[ggplot2:ggplot2]{ggplot2} layer objects that can be added to a phase plane plot with \code{+}.
}
\details{
The manifolds are computed by:
\enumerate{
\item Finding the eigenvectors of the Jacobian at the saddle point.
\item Placing seed points at distance \code{epsilon} from the saddle along
each eigenvector (both + and - directions, giving four seeds total).
\item Integrating \strong{forward} from the two unstable seeds to trace the
unstable manifold.
\item Integrating \strong{backward} from the two stable seeds to trace the
stable manifold.
}

If a \code{\link[=classify_equilibrium]{classify_equilibrium()}} result is supplied via
\code{eq_classified}, its Jacobian entries are reused to avoid redundant
computation. Otherwise the Jacobian is computed internally via finite
differences.
}
\examples{
# Saddle at the origin of example_08: dx/dt = x, dy/dt = -y
gg_flow_field(ode_example_08,
              xlim = c(-3, 3), ylim = c(-3, 3)) +
  gg_manifolds(ode_example_08, equilibrium = c(0, 0))

# Using pre-computed classify_equilibrium() output
eq_cl <- classify_equilibrium(ode_example_08, equilibrium = c(0, 0))
gg_flow_field(ode_example_08,
              xlim = c(-3, 3), ylim = c(-3, 3)) +
  gg_manifolds(ode_example_08,
               equilibrium   = c(0, 0),
               eq_classified = eq_cl)

# Draw only the unstable manifold
gg_flow_field(ode_example_08,
              xlim = c(-3, 3), ylim = c(-3, 3)) +
  gg_manifolds(ode_example_08,
               equilibrium   = c(0, 0),
               draw_stable   = FALSE,
               draw_unstable = TRUE)

# Full workflow: find, classify, then draw manifolds
eq      <- find_equilibrium(ode_example_11, y0 = c(0.8, 0.8))
eq_cl   <- classify_equilibrium(ode_example_11, equilibrium = eq[[1L]])

gg_flow_field(ode_example_11,
              xlim = c(0, 4), ylim = c(0, 3)) +
  gg_nullclines(ode_example_11,
                xlim = c(0, 4), ylim = c(0, 3)) +
  gg_manifolds(ode_example_11,
               equilibrium   = eq[[1L]],
               eq_classified = eq_cl,
               t_manifold    = 5)

}
\seealso{
\code{\link[=find_equilibrium]{find_equilibrium()}}, \code{\link[=classify_equilibrium]{classify_equilibrium()}},
\code{\link[=gg_flow_field]{gg_flow_field()}}, \code{\link[=gg_trajectory]{gg_trajectory()}}
}
