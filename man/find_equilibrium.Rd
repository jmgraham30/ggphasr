% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_equilibrium.R
\name{find_equilibrium}
\alias{find_equilibrium}
\title{Find equilibria of an ODE system numerically}
\usage{
find_equilibrium(
  deriv,
  y0 = NULL,
  system = c("two.dim", "one.dim"),
  parameters = NULL,
  xlim = NULL,
  ylim = NULL,
  n_grid = 10L,
  tol = 1e-08,
  dedup_tol = 1e-04
)
}
\arguments{
\item{deriv}{A function describing the ODE system, in Convention A or B.
See \link{ggphasr} for details.}

\item{y0}{Initial guess(es) for the root-finder. One of:
\itemize{
\item A numeric vector of length 1 (1D) or 2 (2D): a single starting
point.
\item A numeric matrix with one row per starting point.
\item A list of numeric vectors.
\item \code{NULL}: triggers automatic grid search over \code{xlim} × \code{ylim}
(requires \code{xlim} and \code{ylim} to be supplied).
}}

\item{system}{Character: \code{"two.dim"} (default) or \code{"one.dim"}.}

\item{parameters}{Parameter vector or list passed to \code{deriv}.}

\item{xlim}{Numeric vector of length 2. x-axis search range. Required
when \code{y0 = NULL}. Default: \code{NULL}.}

\item{ylim}{Numeric vector of length 2. y-axis search range. Required
when \code{y0 = NULL}. Default: \code{NULL}.}

\item{n_grid}{Integer. Grid resolution per axis for automatic search
when \code{y0 = NULL}. Default: \code{10} (giving up to 100 starting points
for 2D systems).}

\item{tol}{Numeric. Convergence tolerance passed to
\code{\link[rootSolve:multiroot]{rootSolve::multiroot()}}. Default: \code{1e-8}.}

\item{dedup_tol}{Numeric. Distance tolerance for removing duplicate
roots. Two equilibria closer than this are treated as the same point.
Default: \code{1e-4}.}
}
\value{
A list of numeric vectors, each giving the coordinates of one
equilibrium point. If only one equilibrium is found, a list of length
1 is returned (not an unwrapped vector) for consistency.
}
\description{
Locates one or more equilibrium points of a one- or two-dimensional
autonomous ODE system using Newton-Raphson root-finding via
\code{\link[rootSolve:multiroot]{rootSolve::multiroot()}}. Accepts either a single initial guess or a
grid of starting points to search for multiple equilibria.
}
\details{
Newton-Raphson is sensitive to the initial guess — it may fail to
converge or converge to a non-equilibrium point for badly chosen
starting values. When \code{y0 = NULL}, the function tries \code{n_grid^2} starting
points on a regular grid, collects all converged roots, and deduplicates
them. This is more reliable than a single guess but still not exhaustive.

Failed convergences are silently discarded; only roots where the
residual is below \code{tol * 100} are retained.
}
\examples{
# Single guess: find the interior equilibrium of Lotka-Volterra
find_equilibrium(
  ode_lotka_volterra,
  y0         = c(1.5, 1.5),
  parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
)

# Automatic grid search: find all equilibria of example 11
find_equilibrium(
  ode_example_11,
  y0    = NULL,
  xlim  = c(0, 4),
  ylim  = c(0, 4)
)

# 1D system: find equilibria of the logistic equation
find_equilibrium(
  ode_logistic,
  y0         = NULL,
  system     = "one.dim",
  ylim       = c(-1, 12),
  parameters = c(r = 1, K = 10)
)

}
\seealso{
\code{\link[=classify_equilibrium]{classify_equilibrium()}}
}
