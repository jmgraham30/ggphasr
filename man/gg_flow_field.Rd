% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow_field.R
\name{gg_flow_field}
\alias{gg_flow_field}
\title{Plot a flow field (direction field) for an ODE system}
\usage{
gg_flow_field(
  deriv,
  xlim,
  ylim,
  system = c("two.dim", "one.dim"),
  parameters = NULL,
  n_points = 21L,
  arrow_type = c("equal", "proportional"),
  arrow_color = "grey60",
  arrow_size = 0.25,
  arrow_linewidth = 0.4,
  arrow_length_scale = 0.85,
  max_magnitude = NULL,
  color_by_magnitude = FALSE,
  magnitude_palette = c("grey80", "#2c7bb6"),
  add_origin_lines = TRUE,
  origin_line_color = "grey40",
  xlab = NULL,
  ylab = "y",
  title = NULL
)
}
\arguments{
\item{deriv}{A function (or named list of functions) describing the ODE
system, in either Convention A (\code{f(t, y, parameters)} returning
\code{list(c(...))}) or Convention B (simplified \code{f(x, y, parameters)}
returning \code{c(...)}). See \link{ggphasr-package} for details on ODE conventions.

To overlay multiple systems, pass a named list:
\code{deriv = list(system1 = f1, system2 = f2)}.}

\item{xlim}{Numeric vector of length 2. Range of the x-axis (or the
time axis for 1D systems). Required.}

\item{ylim}{Numeric vector of length 2. Range of the y-axis (state
variable axis). Required.}

\item{system}{Character string: \code{"two.dim"} (default) or \code{"one.dim"}.}

\item{parameters}{A numeric vector or list of parameter values passed to
\code{deriv}. When \code{deriv} is a list of functions, \code{parameters} can be a
named list of parameter vectors, one per system; or a single vector
applied to all systems.}

\item{n_points}{Integer. Number of grid points along each axis.
Default: \code{21} (matching phaseR's default).}

\item{arrow_type}{Character string: \code{"equal"} (default, all arrows the
same length, showing direction only) or \code{"proportional"} (arrow length
proportional to vector magnitude).}

\item{arrow_color}{Character. Color of the arrows. Default: \code{"grey60"}.
Ignored when \code{color_by_magnitude = TRUE}.}

\item{arrow_size}{Numeric. Relative size of the arrow heads.
Default: \code{0.25}.}

\item{arrow_linewidth}{Numeric. Line width of the arrow shafts.
Default: \code{0.4}.}

\item{arrow_length_scale}{Numeric in (0, 1]. Maximum arrow length as a
fraction of the grid cell size. Default: \code{0.85}.}

\item{max_magnitude}{Numeric or \code{NULL}. For \code{arrow_type = "proportional"}
only: if supplied, arrow lengths are scaled relative to this reference
magnitude rather than the maximum on the grid. Useful for producing
consistent scaling across multiple plots. Default: \code{NULL} (auto-scale
to grid maximum).}

\item{color_by_magnitude}{Logical. If \code{TRUE}, arrows are colored by
vector magnitude using a continuous color scale. Overrides
\code{arrow_color}. Default: \code{FALSE}.}

\item{magnitude_palette}{Character vector of length 2. Low and high colors
for the magnitude color scale, used when \code{color_by_magnitude = TRUE}.
Default: \code{c("grey80", "#2c7bb6")}.}

\item{add_origin_lines}{Logical. If \code{TRUE} (default), adds thin reference
lines at \code{x = 0} and \code{y = 0} (or \code{y = 0} only for 1D systems) via
\code{\link[ggplot2:geom_hline]{ggplot2::geom_hline()}} and \code{\link[ggplot2:geom_vline]{ggplot2::geom_vline()}}.}

\item{origin_line_color}{Character. Color of the origin reference lines.
Default: \code{"grey40"}.}

\item{xlab}{Character. x-axis label. Default: \code{"x"} for 2D systems,
\code{"t"} for 1D systems.}

\item{ylab}{Character. y-axis label. Default: \code{"y"}.}

\item{title}{Character or \code{NULL}. Plot title. Default: \code{NULL}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object. Additional layers, scales, and theme
elements can be added with \code{+}.
}
\description{
Computes and plots the direction or velocity field of a one- or
two-dimensional autonomous ODE system on a regular grid. Returns a
complete \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object to which additional layers (nullclines,
trajectories, etc.) can be added with \code{+}.
}
\details{
Multiple ODE systems can be overlaid on a single plot by passing a named
list to the \code{deriv} argument (see Details and Examples).
\subsection{ODE conventions}{

Both phaseR-style (Convention A) and simplified (Convention B) ODE
functions are accepted. The calling convention is detected automatically
from the function's argument names.
}

\subsection{Multiple systems}{

When \code{deriv} is a named list, each system is drawn with a different color
(taken from a discrete palette) and a legend is added automatically.
\code{parameters} should then be a named list of the same length as \code{deriv},
with one parameter vector per system. If \code{parameters} is a single vector,
it is applied to all systems.
}

\subsection{1D systems}{

For \code{system = "one.dim"}, the x-axis represents time (the independent
variable) and the y-axis represents the state variable. Arrows point
rightward (increasing time) and up or down according to \code{dy/dt}.
}
}
\examples{
# 2D system: Lotka-Volterra phase plane
gg_flow_field(
  ode_lotka_volterra,
  xlim       = c(0, 5),
  ylim       = c(0, 5),
  parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
)

# 1D system: logistic growth phase portrait
gg_flow_field(
  ode_logistic,
  xlim       = c(0, 4),
  ylim       = c(-1, 12),
  system     = "one.dim",
  parameters = c(r = 1, K = 10)
)

# Proportional arrows colored by magnitude
gg_flow_field(
  ode_van_der_pol,
  xlim                = c(-3, 3),
  ylim                = c(-3, 3),
  parameters          = c(mu = 1),
  arrow_type          = "proportional",
  color_by_magnitude  = TRUE
)

# Compose with nullclines and a custom title
gg_flow_field(
  ode_lotka_volterra,
  xlim       = c(0, 5),
  ylim       = c(0, 5),
  parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1),
  title      = "Lotka-Volterra"
) +
  ggplot2::labs(x = "Prey", y = "Predator")

}
\seealso{
\code{\link[=gg_nullclines]{gg_nullclines()}}, \code{\link[=gg_trajectory]{gg_trajectory()}},
\code{\link[=gg_phase_portrait]{gg_phase_portrait()}}, \code{\link[=theme_phase_plane]{theme_phase_plane()}}
}
