% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_systems_2d.R
\name{ode_lotka_volterra}
\alias{ode_lotka_volterra}
\title{Lotka-Volterra predator-prey model}
\usage{
ode_lotka_volterra(
  t,
  y,
  parameters = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
)
}
\arguments{
\item{t}{Numeric scalar. Time (autonomous system; included for deSolve
compatibility).}

\item{y}{Numeric vector of length 2. State vector: \eqn{y[1]} = prey
(\eqn{x}), \eqn{y[2]} = predator.}

\item{parameters}{Named numeric vector with elements:
\describe{
\item{\code{alpha}}{Prey growth rate. Default: \code{1}.}
\item{\code{beta}}{Predation rate. Default: \code{0.5}.}
\item{\code{delta}}{Predator growth rate per prey consumed. Default: \code{0.5}.}
\item{\code{gamma}}{Predator death rate. Default: \code{1}.}
}}
}
\value{
A list with one element: a numeric vector of length 2 containing
\eqn{(dx/dt, dy/dt)}, as required by \code{\link[deSolve:ode]{deSolve::ode()}}.
}
\description{
The classic two-species predator-prey system:
}
\details{
\deqn{\frac{dx}{dt} = \alpha x - \beta x y}
\deqn{\frac{dy}{dt} = \delta x y - \gamma y}

where \eqn{x(t)} is prey abundance, \eqn{y(t)} is predator abundance,
\eqn{\alpha > 0} is the prey growth rate, \eqn{\beta > 0} is the
predation rate, \eqn{\delta > 0} is the predator growth rate per prey
consumed, and \eqn{\gamma > 0} is the predator death rate.

The system has two equilibria: a trivial unstable equilibrium at
\eqn{(0, 0)} and a neutrally stable center at
\eqn{(x^*, y^*) = (\gamma/\delta,\; \alpha/\beta)}.
All trajectories starting in the positive quadrant are closed orbits
around the interior equilibrium.
}
\examples{
# Interior equilibrium at (gamma/delta, alpha/beta) = (2, 2)
ode_lotka_volterra(t = 0, y = c(2, 2),
                  parameters = c(alpha = 1, beta = 0.5,
                                 delta = 0.5, gamma = 1))

\dontrun{
gg_flow_field(ode_lotka_volterra, xlim = c(0, 5), ylim = c(0, 5))
}

}
\seealso{
\code{\link[=ode_competition]{ode_competition()}}
}
