% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_systems_1d.R
\name{ode_logistic}
\alias{ode_logistic}
\title{Logistic growth model}
\usage{
ode_logistic(t, y, parameters = c(r = 1, K = 10))
}
\arguments{
\item{t}{Numeric scalar. Time (not used directly, included for
deSolve compatibility).}

\item{y}{Numeric vector of length 1. Current state: \eqn{y[1]} is the
population size.}

\item{parameters}{Named numeric vector with elements:
\describe{
\item{\code{r}}{Intrinsic growth rate. Default: \code{1}.}
\item{\code{K}}{Carrying capacity. Default: \code{10}.}
}}
}
\value{
A list with one element: a numeric vector of length 1 containing
\eqn{dy/dt}, as required by \code{\link[deSolve:ode]{deSolve::ode()}}.
}
\description{
The ODE for logistic (Verhulst) population growth:
}
\details{
\deqn{\frac{dy}{dt} = r \, y \left(1 - \frac{y}{K}\right)}

where \eqn{y(t)} is the population size, \eqn{r > 0} is the intrinsic
growth rate, and \eqn{K > 0} is the carrying capacity. The model has
two equilibria: an unstable equilibrium at \eqn{y = 0} and a stable
equilibrium at \eqn{y = K}.

The analytic solution is
\eqn{y(t) = K / (1 + ((K - y_0)/y_0) \, e^{-rt})}.
}
\examples{
# Evaluate at y = 5 with default parameters (r = 1, K = 10)
# dy/dt = 1 * 5 * (1 - 5/10) = 2.5
ode_logistic(t = 0, y = c(5), parameters = c(r = 1, K = 10))

\dontrun{
gg_phase_portrait(ode_logistic, ylim = c(-2, 12))
}

}
\seealso{
\code{\link[=ode_exponential]{ode_exponential()}}, \code{\link[=ode_monomolecular]{ode_monomolecular()}}
}
