% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory.R
\name{gg_trajectory}
\alias{gg_trajectory}
\title{Add solution trajectories to a phase plane plot}
\usage{
gg_trajectory(
  deriv,
  y0,
  xlim,
  ylim,
  system = c("two.dim", "one.dim"),
  parameters = NULL,
  t_end = 10,
  t_start_back = NULL,
  t_steps = 500L,
  method = "lsoda",
  color = NULL,
  linewidth = 0.7,
  add_arrows = TRUE,
  arrow_size = 0.3,
  add_start_point = TRUE,
  start_point_size = 2
)
}
\arguments{
\item{deriv}{A function describing the ODE system, in Convention A or B.
See \link{ggphasr} for details.}

\item{y0}{Initial condition(s). One of:
\itemize{
\item A numeric vector of length 1 (1D) or 2 (2D) for a single
initial condition.
\item A numeric matrix with one row per initial condition (columns
are state variables).
\item A list of numeric vectors, one per initial condition.
}}

\item{xlim}{Numeric vector of length 2. x-axis range. Used to clip
trajectories that leave the plot area.}

\item{ylim}{Numeric vector of length 2. y-axis range.}

\item{system}{Character: \code{"two.dim"} (default) or \code{"one.dim"}.}

\item{parameters}{Parameter vector or list passed to \code{deriv}.}

\item{t_end}{Numeric. End time for forward integration. Default: \code{10}.}

\item{t_start_back}{Numeric or \code{NULL}. End time for backward integration
(should be negative). If \code{NULL} (default), only forward integration
is performed. Set to a negative value (e.g., \code{-10}) to also integrate
backward from each initial condition.}

\item{t_steps}{Integer. Number of time steps per integration direction.
Default: \code{500}. Increase for smoother curves on long integrations.}

\item{method}{Character. deSolve integration method. Default: \code{"lsoda"}
(automatically switches between stiff and non-stiff solvers).}

\item{color}{Character or \code{NULL}. Trajectory color. If \code{NULL} and there
are multiple initial conditions, each trajectory gets a different color
from a discrete palette. If a single color string (e.g., \code{"black"}),
all trajectories share that color. Default: \code{NULL}.}

\item{linewidth}{Numeric. Trajectory line width. Default: \code{0.7}.}

\item{add_arrows}{Logical. If \code{TRUE} (default), adds an arrow head at the
midpoint of each trajectory segment showing the direction of flow.}

\item{arrow_size}{Numeric. Size of the direction arrow heads in lines.
Default: \code{0.3}.}

\item{add_start_point}{Logical. If \code{TRUE} (default), marks each initial
condition with a filled circle.}

\item{start_point_size}{Numeric. Size of the initial condition point.
Default: \code{2}.}
}
\value{
A list of \link[ggplot2:ggplot2]{ggplot2} layer objects. Add to a ggplot with \code{+}.
}
\description{
Numerically integrates and plots one or more solution trajectories of a
one- or two-dimensional autonomous ODE system. Returns a list of
\link[ggplot2:ggplot2]{ggplot2} layer objects that can be added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object
with \code{+}.
}
\examples{
# Single trajectory on a Lotka-Volterra phase plane
lv_params <- c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)

gg_flow_field(ode_lotka_volterra,
              xlim = c(0, 5), ylim = c(0, 5),
              parameters = lv_params) +
  gg_trajectory(ode_lotka_volterra,
                y0         = c(1, 1),
                xlim       = c(0, 5),
                ylim       = c(0, 5),
                parameters = lv_params)

# Multiple initial conditions from a matrix
ics <- matrix(c(0.5, 0.5,
                1.0, 2.0,
                3.0, 1.0), ncol = 2, byrow = TRUE)

gg_flow_field(ode_lotka_volterra,
              xlim = c(0, 5), ylim = c(0, 5),
              parameters = lv_params) +
  gg_nullclines(ode_lotka_volterra,
                xlim = c(0, 5), ylim = c(0, 5),
                parameters = lv_params) +
  gg_trajectory(ode_lotka_volterra,
                y0         = ics,
                xlim       = c(0, 5),
                ylim       = c(0, 5),
                parameters = lv_params,
                t_end      = 20)

# Forward and backward integration near a saddle point
gg_flow_field(ode_example_08,
              xlim = c(-3, 3), ylim = c(-3, 3)) +
  gg_trajectory(ode_example_08,
                y0           = c(0.1, 2),
                xlim         = c(-3, 3),
                ylim         = c(-3, 3),
                t_end        = 3,
                t_start_back = -3)

}
\seealso{
\code{\link[=gg_flow_field]{gg_flow_field()}}, \code{\link[=gg_nullclines]{gg_nullclines()}}
}
