% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_phase_plane.R
\name{theme_phase_plane}
\alias{theme_phase_plane}
\title{Phase plane ggplot2 theme}
\usage{
theme_phase_plane(
  base_size = 13,
  base_family = "",
  grid_color = "grey88",
  axis_text_color = "grey30"
)
}
\arguments{
\item{base_size}{Numeric. Base font size in points. Default: \code{13}.}

\item{base_family}{Character. Base font family. Default: \code{""} (device
default).}

\item{grid_color}{Character. Color of the major grid lines.
Default: \code{"grey88"}.}

\item{axis_text_color}{Character. Color of axis tick labels.
Default: \code{"grey30"}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object that can be added to any ggplot with
\code{+}.
}
\description{
A clean \code{\link[ggplot2:theme]{ggplot2::theme()}} designed for phase plane and phase portrait
plots. Key features:
}
\details{
\itemize{
\item White panel background for clean printing and projection
\item Light grey major grid lines to help read off coordinates
\item No panel border (axis lines through the origin are added separately
as \code{\link[ggplot2:geom_hline]{ggplot2::geom_hline()}} / \code{\link[ggplot2:geom_vline]{ggplot2::geom_vline()}} layers by the
\verb{gg_*} plotting functions)
\item Slightly larger base font size than the ggplot2 default, suitable
for classroom projection
}
}
\examples{
library(ggplot2)

# Apply to any ggplot
ggplot(data.frame(x = c(-2, 2), y = c(-2, 2)), aes(x, y)) +
  geom_blank() +
  theme_phase_plane()

# Override font size for a smaller plot
ggplot(data.frame(x = c(-2, 2), y = c(-2, 2)), aes(x, y)) +
  geom_blank() +
  theme_phase_plane(base_size = 10)

# Customize grid color
ggplot(data.frame(x = c(-2, 2), y = c(-2, 2)), aes(x, y)) +
  geom_blank() +
  theme_phase_plane(grid_color = "grey75")

}
\seealso{
\code{\link[ggplot2:theme]{ggplot2::theme()}}, \code{\link[ggplot2:theme_bw]{ggplot2::theme_bw()}}
}
