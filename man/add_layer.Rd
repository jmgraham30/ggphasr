% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase_plane.R
\name{add_layer}
\alias{add_layer}
\title{Add ggplot2 layers to a ggphasr_result object}
\usage{
add_layer(result, layer)
}
\arguments{
\item{result}{A \code{ggphasr_result} object from \code{\link[=gg_phase_plane]{gg_phase_plane()}}.}

\item{layer}{A ggplot2 layer, scale, theme, or list thereof.}
}
\value{
A \code{ggphasr_result} object with the updated \verb{$plot}.
}
\description{
Adds a ggplot2 layer, scale, or theme element to the \verb{$plot} component
of a \code{\link[=gg_phase_plane]{gg_phase_plane()}} result, returning an updated
\code{ggphasr_result} object. This is the recommended way to further
customize a phase plane plot while preserving the \verb{$equilibria} table:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- gg_phase_plane(ode_lotka_volterra, ...)

# Use add_layer() to customize and keep the ggphasr_result structure:
result2 <- add_layer(result, ggplot2::labs(title = "My plot"))
result2$plot        # updated plot
result2$equilibria  # equilibria preserved

# Or extract $plot and add layers with + directly:
result$plot + ggplot2::labs(title = "My plot")
}\if{html}{\out{</div>}}
}
