% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_systems_2d.R
\name{ode_toggle}
\alias{ode_toggle}
\title{Genetic toggle switch model}
\usage{
ode_toggle(t, y, parameters = c(alpha1 = 3, alpha2 = 3, beta = 2, gamma = 2))
}
\arguments{
\item{t}{Numeric scalar. Time (autonomous; included for deSolve
compatibility).}

\item{y}{Numeric vector of length 2. State vector: \eqn{y[1]} = \eqn{u},
\eqn{y[2]} = \eqn{v}.}

\item{parameters}{Named numeric vector with elements:
\describe{
\item{\code{alpha1}}{Effective synthesis rate of \eqn{u}. Default: \code{3}.}
\item{\code{alpha2}}{Effective synthesis rate of \eqn{v}. Default: \code{3}.}
\item{\code{beta}}{Cooperativity coefficient in \eqn{u} equation.
Default: \code{2}.}
\item{\code{gamma}}{Cooperativity coefficient in \eqn{v} equation.
Default: \code{2}.}
}}
}
\value{
A list with one element: a numeric vector of length 2 containing
\eqn{(du/dt, dv/dt)}, as required by \code{\link[deSolve:ode]{deSolve::ode()}}.
}
\description{
The Gardner et al. (2000) mutual repressor model of a synthetic genetic
toggle switch:
}
\details{
\deqn{\frac{du}{dt} = \frac{\alpha_1}{1 + v^\beta} - u}
\deqn{\frac{dv}{dt} = \frac{\alpha_2}{1 + u^\gamma} - v}

where \eqn{u} and \eqn{v} are the concentrations of two mutually
repressing proteins. \eqn{\alpha_1, \alpha_2 > 0} are the effective
synthesis rates (incorporating promoter strength and degradation), and
\eqn{\beta, \gamma > 0} are the cooperativity (Hill) coefficients.

For sufficiently large \eqn{\alpha_1} and \eqn{\alpha_2} with
cooperativity \eqn{> 1}, the system is bistable: two stable equilibria
(each corresponding to one protein being dominant) separated by an
unstable saddle point. This bistability is the basis of a synthetic
biological memory device.
}
\examples{
ode_toggle(t = 0, y = c(2, 0.5),
           parameters = c(alpha1 = 3, alpha2 = 3, beta = 2, gamma = 2))

\dontrun{
gg_flow_field(ode_toggle, xlim = c(0, 4), ylim = c(0, 4))
}

}
\references{
Gardner TS, Cantor CR, Collins JJ (2000). Construction of a genetic
toggle switch in Escherichia coli. \emph{Nature} 403: 339–342.
\doi{10.1038/35002131}
}
