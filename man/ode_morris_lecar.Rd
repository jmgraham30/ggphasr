% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_systems_2d.R
\name{ode_morris_lecar}
\alias{ode_morris_lecar}
\title{Morris-Lecar neuron model}
\usage{
ode_morris_lecar(
  t,
  y,
  parameters = c(I = 0, C = 20, gCa = 4.4, gK = 8, gL = 2, VCa = 120, VK = -84, VL = -60,
    V1 = -1.2, V2 = 18, V3 = 2, V4 = 30, phi = 0.04)
)
}
\arguments{
\item{t}{Numeric scalar. Time (autonomous; included for deSolve
compatibility).}

\item{y}{Numeric vector of length 2. State vector: \eqn{y[1]} = membrane
potential \eqn{V} (mV), \eqn{y[2]} = K\eqn{^+} channel open probability
\eqn{N} (dimensionless, \eqn{0 \leq N \leq 1}).}

\item{parameters}{Named numeric vector with elements:
\describe{
\item{\code{I}}{Applied current (\eqn{\mu}A/cm\eqn{^2}). Default: \code{0.}}
\item{\code{C}}{Membrane capacitance (\eqn{\mu}F/cm\eqn{^2}). Default: \code{20}.}
\item{\code{gCa}}{Maximum Ca\eqn{^{2+}} conductance (mS/cm\eqn{^2}).
Default: \code{4.4}.}
\item{\code{gK}}{Maximum K\eqn{^+} conductance (mS/cm\eqn{^2}).
Default: \code{8}.}
\item{\code{gL}}{Leak conductance (mS/cm\eqn{^2}). Default: \code{2}.}
\item{\code{VCa}}{Ca\eqn{^{2+}} reversal potential (mV). Default: \code{120}.}
\item{\code{VK}}{K\eqn{^+} reversal potential (mV). Default: \code{-84}.}
\item{\code{VL}}{Leak reversal potential (mV). Default: \code{-60}.}
\item{\code{V1}}{Voltage at half-activation of Ca\eqn{^{2+}} (mV).
Default: \code{-1.2}.}
\item{\code{V2}}{Slope of Ca\eqn{^{2+}} activation (mV). Default: \code{18}.}
\item{\code{V3}}{Voltage at half-activation of K\eqn{^+} (mV).
Default: \code{2}.}
\item{\code{V4}}{Slope of K\eqn{^+} activation (mV). Default: \code{30}.}
\item{\code{phi}}{Reference frequency (dimensionless). Default: \code{0.04}.}
}}
}
\value{
A list with one element: a numeric vector of length 2 containing
\eqn{(dV/dt, dN/dt)}, as required by \code{\link[deSolve:ode]{deSolve::ode()}}.
}
\description{
The Morris-Lecar (1981) conductance-based neuron model, a 2D reduction of
Hodgkin-Huxley dynamics for a barnacle muscle fiber:
}
\details{
\deqn{C \frac{dV}{dt} = I - g_{Ca} M_\infty(V)(V - V_{Ca}) - g_K N (V - V_K) - g_L (V - V_L)}
\deqn{\frac{dN}{dt} = \phi \frac{N_\infty(V) - N}{\tau_N(V)}}

where the voltage-dependent steady-state functions are:
\deqn{M_\infty(V) = \frac{1}{2}\left(1 + \tanh\!\left(\frac{V - V_1}{V_2}\right)\right)}
\deqn{N_\infty(V) = \frac{1}{2}\left(1 + \tanh\!\left(\frac{V - V_3}{V_4}\right)\right)}
\deqn{\tau_N(V) = \left(\cosh\!\left(\frac{V - V_3}{2 V_4}\right)\right)^{-1}}

The state variables are membrane potential \eqn{V} (mV) and \eqn{N},
the probability that a K\eqn{^+} channel is open. Depending on the
applied current \eqn{I} and other parameters, the system can exhibit
a stable resting state, a stable limit cycle (repetitive firing), or
bistability, making it a rich teaching example for bifurcation analysis.
}
\examples{
# At resting potential with zero applied current
ode_morris_lecar(t = 0, y = c(-60, 0),
                parameters = c(I = 0, C = 20,
                               gCa = 4.4, gK = 8, gL = 2,
                               VCa = 120, VK = -84, VL = -60,
                               V1 = -1.2, V2 = 18, V3 = 2, V4 = 30,
                               phi = 0.04))

\dontrun{
gg_flow_field(ode_morris_lecar,
              xlim = c(-80, 60), ylim = c(0, 0.6))
}

}
\references{
Morris C, Lecar H (1981). Voltage oscillations in the barnacle giant
muscle fiber. \emph{Biophysical Journal} 35(1): 193-213.
\doi{10.1016/S0006-3495(81)84782-0}
}
