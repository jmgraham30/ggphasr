% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_equilibrium.R
\name{classify_equilibrium}
\alias{classify_equilibrium}
\title{Classify an equilibrium point of an ODE system}
\usage{
classify_equilibrium(
  deriv,
  equilibrium,
  system = c("two.dim", "one.dim"),
  parameters = NULL,
  h = 1e-06,
  tol = 1e-08
)
}
\arguments{
\item{deriv}{A function describing the ODE system, in Convention A or B.}

\item{equilibrium}{Numeric vector of length 1 (1D) or 2 (2D) giving the
equilibrium coordinates. Typically obtained from \code{\link[=find_equilibrium]{find_equilibrium()}}.}

\item{system}{Character: \code{"two.dim"} (default) or \code{"one.dim"}.}

\item{parameters}{Parameter vector or list passed to \code{deriv}.}

\item{h}{Numeric. Step size for finite-difference Jacobian computation.
Default: \code{1e-6}.}

\item{tol}{Numeric. Tolerance for treating eigenvalue real/imaginary
parts as zero. Default: \code{1e-8}.}
}
\value{
A \code{\link[=data.frame]{data.frame()}} with one row and the following columns:
\describe{
\item{\code{x}}{x-coordinate of the equilibrium (2D) or \code{NA} (1D).}
\item{\code{y}}{y-coordinate of the equilibrium (both 1D and 2D).}
\item{\code{classification}}{Character. One of: \code{"Stable node"},
\code{"Unstable node"}, \code{"Stable spiral"}, \code{"Unstable spiral"},
\code{"Center"}, \code{"Saddle"}, \code{"Non-isolated equilibrium"} (2D);
or \code{"Stable"}, \code{"Unstable"}, \code{"Inconclusive (df/dy = 0)"} (1D).}
\item{\code{tr}}{Trace of the Jacobian (2D only; \code{NA} for 1D).}
\item{\code{det}}{Determinant of the Jacobian (2D only; \code{NA} for 1D).}
\item{\code{jacobian_11}}{J\link{1,1} — always present.}
\item{\code{jacobian_12}}{J\link{1,2} (2D only; \code{NA} for 1D).}
\item{\code{jacobian_21}}{J\link{2,1} (2D only; \code{NA} for 1D).}
\item{\code{jacobian_22}}{J\link{2,2} (2D only; \code{NA} for 1D).}
\item{\code{lambda_1_re}}{Real part of eigenvalue 1 (2D only; \code{NA} for 1D).}
\item{\code{lambda_1_im}}{Imaginary part of eigenvalue 1 (2D only).}
\item{\code{lambda_2_re}}{Real part of eigenvalue 2 (2D only).}
\item{\code{lambda_2_im}}{Imaginary part of eigenvalue 2 (2D only).}
}
}
\description{
Computes the Jacobian matrix of a one- or two-dimensional ODE system at
a known equilibrium point using forward finite differences, then
classifies the equilibrium using eigenvalue analysis. Returns a tidy
data frame row that can be combined with results from multiple equilibria
using \code{\link[=rbind]{rbind()}}.
}
\details{
Results from multiple equilibria can be combined into a summary table:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{eq_list <- find_equilibrium(ode_example_11, y0 = NULL,
                             xlim = c(0,4), ylim = c(0,4))
results <- do.call(rbind, lapply(eq_list, function(eq) \{
  classify_equilibrium(ode_example_11, equilibrium = eq)
\}))
}\if{html}{\out{</div>}}
}
\examples{
# Classify the interior equilibrium of Lotka-Volterra (expect: Center)
classify_equilibrium(
  ode_lotka_volterra,
  equilibrium = c(2, 2),
  parameters  = c(alpha = 1, beta = 0.5, delta = 0.5, gamma = 1)
)

# Classify all equilibria of example 11
eq_list <- find_equilibrium(ode_example_11, y0 = NULL,
                             xlim = c(0, 4), ylim = c(0, 4))
do.call(rbind, lapply(eq_list, function(eq) {
  classify_equilibrium(ode_example_11, equilibrium = eq)
}))

}
\seealso{
\code{\link[=find_equilibrium]{find_equilibrium()}}
}
