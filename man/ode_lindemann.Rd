% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode_systems_2d.R
\name{ode_lindemann}
\alias{ode_lindemann}
\title{Lindemann mechanism (chemical kinetics)}
\usage{
ode_lindemann(t, y, parameters = c(k1 = 1, k_1 = 1, k2 = 0.5))
}
\arguments{
\item{t}{Numeric scalar. Time (autonomous; included for deSolve
compatibility).}

\item{y}{Numeric vector of length 2. State vector: \eqn{y[1]} = \eqn{[A]},
\eqn{y[2]} = \eqn{[A^*]}.}

\item{parameters}{Named numeric vector with elements:
\describe{
\item{\code{k1}}{Activation rate constant. Default: \code{1}.}
\item{\code{k_1}}{Deactivation rate constant. Default: \code{1}.}
\item{\code{k2}}{Product formation rate constant. Default: \code{0.5}.}
}}
}
\value{
A list with one element: a numeric vector of length 2 containing
\eqn{(d[A]/dt, d[A^*]/dt)}, as required by \code{\link[deSolve:ode]{deSolve::ode()}}.
}
\description{
The Lindemann-Christiansen mechanism for unimolecular gas-phase reactions,
tracking the concentrations of reactant \eqn{A} and activated
intermediate \eqn{A^*}:
}
\details{
Reaction scheme:
\deqn{A + A \xrightarrow{k_1} A^* + A \quad \text{(activation)}}
\deqn{A^* + A \xrightarrow{k_{-1}} A + A \quad \text{(deactivation)}}
\deqn{A^* \xrightarrow{k_2} P \quad \text{(product formation)}}

The resulting ODE system for \eqn{[A]} and \eqn{[A^*]} is:

\deqn{\frac{d[A]}{dt}   = -k_1 [A]^2 + k_{-1} [A^*][A]}
\deqn{\frac{d[A^*]}{dt} =  k_1 [A]^2 - k_{-1} [A^*][A] - k_2 [A^*]}
}
\examples{
ode_lindemann(t = 0, y = c(2, 0.1),
             parameters = c(k1 = 1, k_1 = 1, k2 = 0.5))

\dontrun{
gg_flow_field(ode_lindemann, xlim = c(0, 3), ylim = c(0, 1))
}

}
